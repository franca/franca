package org.genivi

interface Player {
	attribute UInt16 currentTrack
	
	method play {
		in {
			UInt16 trackId
		}
	}
	
	method nextTrack { } 
	method previousTrack { } 

	broadcast endOfPlaylist { }	
	
	contract {
		PSM {
			initial idle
			state idle {
				on call play -> idle
			}
		}
	}
}
